# ===============================================================
# 🧩 БАЗОВЫЕ ЗАДАНИЯ
# ===============================================================

# 1️⃣ Приветствие
# Напиши функцию greet(name), которая выводит:
# "Привет, <имя>!"
# Если имя не передано — использовать "Гость".
def greet(name="Гость"):
    print(f'Привет, {name}!')
greet()


# 2️⃣ Проверка чётности
# Функция check_even(number)
# Если число чётное — вернуть "Чётное", иначе "Нечётное".
def check_even(number):
    if number % 2 == 0:
        return 'Четное'
    else:
        return "Нечетное"
print(check_even(5))

# 3️⃣ Максимум из трёх чисел
# Функция max_of_three(a, b, c) возвращает наибольшее из трёх чисел.
def max_of_three(a, b, c):
    return max(a, b, c)
print(max_of_three(1, 2, 3))


# 4️⃣ Количество символов
# Функция count_chars(text) возвращает количество символов в строке.
# Если текст не передан — вернуть 0.
def count_chars(text=""):
    if text == '' or text == ' ':
        return 0
    else:
        return len(text)
print(count_chars('srww'))


# 5️⃣ Повторение строки
# Функция repeat_text(text, n)
# Возвращает строку, повторённую n раз.
def repeat_text(text, n):
    return text*n
print(repeat_text('abc', 3))


# ===============================================================
# ⚙️ АРГУМЕНТЫ И ЗНАЧЕНИЯ ПО УМОЛЧАНИЮ
# ===============================================================

# 6️⃣ Приветствие с городом
# Функция introduce(name, city="Москва")
# Печатает: "Меня зовут <имя>, я из города <город>."
def introduce(name, city="Москва"):
    print(f'Меня зовут {name}, я из города {city} ')
introduce('Слава', 'Казань')


# 7️⃣ Площадь прямоугольника
# Функция rectangle_area(width, height=1)
# Возвращает площадь (если height не передан — считаем квадрат).
def rectangle_area(width, height='g'):
    if height == 'g':
        return width ** 2
    else:
        return width * height
print(rectangle_area(4, 5))


# 8️⃣ Проверка пароля
# Функция check_password(password, secret="python123")
# Возвращает True, если пароль верный, иначе False.
def check_password(password, secret="python123"):
    if password == secret:
        return True
    else:
        return False
print(check_password(''))


# 9️⃣ Подсчёт слов
# Функция count_words(text)
# Возвращает количество слов в строке (по пробелам).
def count_words(text):
    pass
    "Не понял смысла задания"



# ===============================================================
# 🧠 ПРОДВИНУТЫЕ ЗАДАНИЯ
# ===============================================================

# 1️⃣3️⃣ Таблица умножения
# Функция print_table(n)
# Выводит таблицу умножения для числа n от 1 до 10.
def print_table(n):
    for i in range(1, n+1):
        print(i * n)
print_table(5)


# 1️⃣4️⃣ Фильтр положительных чисел
# Функция filter_positive(numbers)
# Принимает список чисел и возвращает новый список только с положительными.
def filter_positive(numbers):
    for s in numbers:
        if s < 0:
            numbers.remove(s)
    return numbers
print(filter_positive([1, 2, 3, -5]))


# 1️⃣5️⃣ Информация о книге
# Функция book_info(title, author, year=None)
# Возвращает строку:
# "Книга '<title>' (автор: <author>, год: <year>)"
# Если год не указан — писать "год неизвестен".
def book_info(title, author, year='Год неизвестен'):
    return f'Книга {title} (автор: {author}, год: {year})'
print(book_info('Гарри Поттер', "Джоан Роулинг"))

# Задание 2: Функция для нахождения среднего значения. Напишите функцию
# average(numbers), которая принимает список чисел и возвращает их среднее
# значение.

# Задание 3: Нахождение максимума. Напишите функцию find_max(numbers), которая
# принимает список чисел и возвращает наибольшее число в списке.
